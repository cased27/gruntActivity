{
  "name": "tomato-tapper",
  "version": "1.0.0",
  "description": "gruntActivity",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "grunt": "^1.3.0",
    "grunt-contrib-uglify": "^5.0.1",
    "grunt-contrib-watch": "^1.1.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "grunt-babel": "^8.0.0"
  }
}
